spring:
  application:
    name: demoProject

  datasource:
    url: jdbc:mysql://localhost:3306/SecurityTesting?useSSL=false&serverTimezone=UTC
    username: root
    password: f1309D1309@
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties.hibernate:
      format_sql: true

server:
  port: 8080